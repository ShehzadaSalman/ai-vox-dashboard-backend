# AI Receptionist Dashboard Backend

## Environment Variables

Copy this file to `.env` and configure the values:

```bash
# Database
DATABASE_URL="postgresql://username:password@localhost:5432/aivox_dashboard"

# Retell API
RETELL_API_KEY="your_retell_api_key_here"

# Supabase (if using Supabase directly)
SUPABASE_URL="your_supabase_url_here"
SUPABASE_KEY="your_supabase_key_here"

# API Authentication
API_AUTH_KEY="your_api_auth_key_here"
JWT_SECRET="your_jwt_secret_here"

# Server Configuration
PORT=3000
NODE_ENV=development

# Logging
LOG_LEVEL=info
```

## Required Environment Variables

- `DATABASE_URL`: PostgreSQL connection string for your database
- `RETELL_API_KEY`: API key from Retell for accessing call data
- `API_AUTH_KEY`: Secret key for API authentication
 - `JWT_SECRET`: Secret for signing JWT tokens

## Optional Environment Variables

- `PORT`: Server port (default: 3000)
- `NODE_ENV`: Environment mode (development/production)
- `LOG_LEVEL`: Logging level (debug/info/warn/error)
- `SUPABASE_URL`: Supabase project URL (if using Supabase)
- `SUPABASE_KEY`: Supabase API key (if using Supabase)

## Security Notes

- Never commit the `.env` file to version control
- Use strong, unique API keys
- Rotate API keys regularly
- Use environment-specific configurations